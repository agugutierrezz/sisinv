<!DOCTYPE html>
<html>
  <head>
    <title>Sistema de Inventario</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "ui", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="ui-dark">
    <%= turbo_refreshes_with method: :morph, scroll: :preserve %>

      <% unless controller_name == "sessions" && action_name == "new" %> 
      <nav class="navbar">
        <div class="nav-left">
          <%= link_to root_path, class: "brand" do %>
            <%= image_tag("logo.png", alt: "SisInv", class: "brand-logo") rescue nil %>
            <span class="brand-text"></span>
          <% end %>

          <%= link_to "Artículos", articles_path, class: "nav-link" %>
          <%= link_to "Personas",  people_path,   class: "nav-link" %>
          <%= link_to "Nueva transferencia", new_transfer_path, class: "nav-link" %>
          <%= link_to "Catálogo", catalog_path, class: "nav-link" %>
        </div>

        <div class="nav-right">
        <%= link_to "Cerrar Sesión",
              session_path,
              class: "btn btn-ghost",
              data: { turbo_method: :delete, turbo_confirm: "¿Cerrar sesión?" } %>
        </div>
      </nav>
    <% end %>

    <div class="container">
      <%= render "shared/flash" rescue nil %>
      <%= yield %>
    </div>
  </body>
</html>
